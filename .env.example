# ======================================================
# EKIO — Diagnostico EHS — Variables de configuracion
# ======================================================
# Copia este fichero como .env y rellena los valores.
# NUNCA subas .env al repositorio.

# -- n8n -----------------------------------------------
# URL del webhook que recibe el POST del formulario
N8N_WEBHOOK_URL=https://crm-ekio-n8n.idv05l.easypanel.host/webhook/ekio-diagnostico

# -- Klaviyo -------------------------------------------
# Private API Key (Settings -> API Keys en Klaviyo)
KLAVIYO_API_KEY=pk_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ID del flow origen (by EDE) — solo lectura para clonar
KLAVIYO_FLOW_ORIGEN=RQ83ti

# ID del flow destino (TEST n8n) — aqui se recrean las acciones
KLAVIYO_FLOW_DESTINO=WAhmU7

# -- ManyChat ------------------------------------------
# Token de API (pendiente integracion)
MANYCHAT_TOKEN=TU_TOKEN_MANYCHAT

# -- Shopify -------------------------------------------
# Dominio donde se aloja el formulario en produccion
SHOPIFY_DOMAIN=ekio.es

# -- Vercel --------------------------------------------
# URL de produccion tras el primer deploy
VERCEL_URL=https://ekio-formulario.vercel.app
